# DO NOT COMMIT THIS FILE!
# Copy this template to .env.local and fill in your values

# =============================================
# Supabase Configuration
# =============================================
# Get these from: Supabase Dashboard → Project Settings → API

# Project URL (public, safe in client code)
NEXT_PUBLIC_SUPABASE_URL=https://your-project-ref.supabase.co

# Anon Key (public, safe in client code)
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6...

# Service Role Key (SECRET! Server-side only!)
SUPABASE_SERVICE_ROLE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6...

# =============================================
# OpenRouter AI Configuration
# =============================================
# Get API key from: https://openrouter.ai/keys

OPENROUTER_API_KEY=sk-or-v1-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
# Optional split keys (recommended for production deployments)
OPENROUTER_DEV_API_KEY=sk-or-v1-dev-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
OPENROUTER_PROD_API_KEY=sk-or-v1-prod-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# BYOK encryption secret (required if users can store their own API keys)
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
BYOK_ENCRYPTION_SECRET=generate-random-32-byte-hex-string-here

# Daily request guardrail for shared platform budget (set to 0 or leave empty to disable)
OPENROUTER_PLATFORM_DAILY_REQUEST_LIMIT=250
# Soft threshold ratio before hard block (0.1 to 1.0)
OPENROUTER_PLATFORM_SOFT_LIMIT_RATIO=0.9

# Optional model lists (comma-separated) for premium-first routing
OPENROUTER_PREMIUM_MODELS=openai/gpt-4o-mini:paid,mistralai/mistral-7b-instruct:paid
OPENROUTER_FALLBACK_MODELS=meta-llama/llama-3.3-70b-instruct:free,qwen/qwen-3-235b-a22b:free,mistralai/mistral-small-3.1-24b:free,google/gemma-3-4b-instruct:free

# =============================================
# Application Configuration
# =============================================

# App URL (development)
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Node environment
NODE_ENV=development

# =============================================
# Cron Job Security
# =============================================
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# Used to authenticate weekly hint reset cron job

CRON_SECRET=generate-random-32-byte-hex-string-here
